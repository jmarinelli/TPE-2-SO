#include <stdio.h>

filesystem_init() {
	
	/*
	 * TODO: 
	 * 
	 * - Busca la carpeta /cvs en el sistema, si no esta la crea
	 * - Dentro de la carpeta cvs busca la carpeta config con el archivo
	 * 	 con el arbol guardado y lo levanta a memoria, si no esta lo 
	 *   crea.
	 * - 
	 * 
	 * */
	
}
